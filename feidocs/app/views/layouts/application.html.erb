<!DOCTYPE html>
<html>
  <head>
    <title>Feidocs</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
  <p class="navbar-text pull-right">
    <% if professor_signed_in? %>
      Logged in as <strong><%= current_professor.email %></strong>.
      <%= link_to 'Edit profile', edit_professor_registration_path, :class => 'navbar-link' %> |
      <%= link_to "Logout", destroy_professor_session_path, method: :delete, :class => 'navbar-link'  %>
    <% else %>
      <%= link_to "Sign up", new_professor_registration_path, :class => 'navbar-link'  %> |
      <%= link_to "Login", new_professor_session_path, :class => 'navbar-link'  %>
    <% end %>
  </p>
  <ul class="nav">
    <li class="active"><a href="/ideas">Ideas</a></li>
  </ul>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <% if notice %>
      <p class="alert alert-success"><%= notice %></p>
    <% end %>
    <% if alert %>
      <p class="alert alert-danger"><%= alert %></p>
    <% end %>  
      <%= yield %>
  </body>
</html>
